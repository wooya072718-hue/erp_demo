# 인사·총무 실무 학습 앱 — 기획서

> ⚠️ **교육용 가상 자료**
> 이 문서와 앱에 등장하는 회사·직원·급여·자산 정보는 학습 목적으로 처음부터 지어낸
> 가상 사례입니다. 실존하는 회사·개인과 아무 관련이 없으며, 실제 인사 결정이나
> 법적 판단의 근거로 사용할 수 없습니다.

---

## 1. 왜 만드는가

인사·총무 담당자가 실제 업무 시스템을 켜지 않고도 **근태·휴가 처리**와
**비품 불출·반납 관리**의 흐름을 연습할 수 있게 하는 것이 목적입니다.
실수해도 안전하고, 언제든 초기 상태로 되돌릴 수 있어야 합니다.

## 2. 가상 회사 설정

| 항목 | 내용 |
| --- | --- |
| 회사명 | (주)한빛상사 *(가상)* |
| 업종 | 생활용품 도소매 |
| 임직원 수 | 24명 |
| 부서 | 경영지원팀 · 영업1팀 · 영업2팀 · 개발팀 · 디자인팀 · 물류팀 |
| 기준 시점 | 2026년 9월 |

## 3. 담을 기능 (확정)

### 3-1. 근태 관리
- 월별 근태 현황표 (직원 × 일자)
- 상태 구분: 정상 / 지각 / 조퇴 / 결근 / 연차 / 반차 / 출장
- 직원별 집계: 출근일수, 지각횟수, 연차사용일수
- 부서·이름으로 필터

### 3-2. 휴가 관리
- 직원별 연차 부여일수 / 사용 / 잔여 조회
- 휴가 신청 목록과 **결재(승인 / 반려)** 처리
- 신규 휴가 신청 등록 (잔여 연차 초과 시 경고)
- 승인된 휴가는 근태 현황표에 자동 반영

### 3-3. 총무 — 자산·비품 관리
- 비품 대장: 자산코드, 품목, 규격, 총수량, 불출수량, 가용수량, 보관위치
- 재고 부족 경고 (가용수량 0)

### 3-4. 총무 — 불출·반납
- 누가 / 무엇을 / 언제 빌려갔는지 이력 관리
- 반납 예정일 초과 건 **반납 지연** 표시
- 신규 불출 등록, 반납 처리

### 3-5. 대시보드
위 네 가지에서 "오늘 당장 봐야 할 것"만 모은 첫 화면.
오늘 근태 요약 / 결재 대기 휴가 건수 / 반납 지연 건수 / 재고 부족 품목.

## 4. 만들지 않는 것 (이번 범위 밖)

- 급여 계산, 4대보험, 연말정산
- 사원 채용·평가
- 실제 이메일·알림 발송
- 로그인 / 권한 분리 (교육용이므로 모두 관리자 시점)

## 5. 기술 구조

```
erp_demo/
├── index.html        ← 앱 본체. 더블클릭하면 브라우저에서 바로 실행
├── data/             ← 가상 자료 원본 (JSON). 열어서 읽고 고칠 수 있음
│   ├── employees.json
│   ├── leaves.json
│   ├── assets.json
│   └── loans.json
├── PLAN.md           ← 이 기획서
└── README.md         ← 사용 설명서
```

- **단일 HTML 파일**: 설치·서버 불필요. 인터넷이 없어도 동작.
- **초기 가상 데이터**는 `index.html` 안에 들어 있고, 같은 내용이 `data/*.json`으로도
  들어 있어 자료만 따로 보거나 고칠 수 있습니다.
- **사용자가 입력·수정한 내용**은 브라우저 저장소(localStorage)에 남습니다.
  새로고침해도 유지되고, `초기화` 버튼으로 언제든 처음 상태로 되돌립니다.
- **JSON 내려받기 / 불러오기** 버튼으로 실습 결과를 파일로 주고받을 수 있습니다.

## 6. 화면 구조

```
┌──────────────────── 교육용 가상 자료 안내 띠 ────────────────────┐
├────────┬──────────────────────────────────────────────────────┤
│ 대시보드 │                                                      │
│ 근태현황 │            선택한 메뉴의 내용 영역                    │
│ 휴가관리 │            (요약 카드 + 표 + 입력 폼)                 │
│ 비품대장 │                                                      │
│ 불출반납 │                                                      │
└────────┴──────────────────────────────────────────────────────┘
```

## 7. 만드는 순서

1. 가상 데이터 설계 및 생성 (직원 24명 · 비품 14종 · 휴가신청 18건 · 불출 16건)
2. 화면 뼈대와 공통 스타일
3. 대시보드 → 근태 → 휴가 → 비품 → 불출반납 순으로 구현
4. 저장·초기화·내보내기 기능
5. `data/*.json` 추출 및 사용 설명서 작성
