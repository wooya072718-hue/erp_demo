# 개발 순서

> ⚠️ **교육용 가상 자료 프로젝트**
> 근거 문서: `PRD.md` (요구사항) · `design.md` (Apple 디자인 분석)
> 작성일 2026-09-14

---

## 0. 지금까지 끝난 것

| 구분 | 상태 |
| --- | --- |
| 화면 5개 (대시보드·근태·휴가·비품·불출반납) | ✅ 동작 |
| 실습 미션 8개 자동 채점 | ✅ 브라우저에서 **8/8 달성 확인** |
| 저장·초기화·JSON 내보내기 | ✅ 새로고침 유지 / 초기화 복원 확인 |
| `design.md` 디자인 언어 | ✅ 주요 규칙 적용 (아래 4번 표) |
| 가상 자료 2벌 | ✅ 앱 내장 24명 / 학습용 12명 JSON |

**브라우저 실측 검증 결과**

| 확인 항목 | 결과 |
| --- | --- |
| 사유 없이 반려 시도 | 차단됨 ✅ |
| 잔여 12일인데 75일 신청 | `신청 75일 > 남은 연차 12일` 경고 + 등록 차단 ✅ |
| 가용 10개인데 9999개 불출 | `가용수량(10)보다 많이 불출할 수 없습니다` 차단 ✅ |
| 이상 근태 필터 | 24명 → 4명 ✅ |
| 재고 부족 필터 | 14종 → 2종 ✅ |
| 새로고침 | 미션 8/8 유지 ✅ |
| 초기화 | 결재대기 8건 · 반납지연 3건 · 재고부족 1종 = **PRD 6-2와 일치** ✅ |

---

## 1. 순서를 이렇게 정한 이유

개발 순서는 "하고 싶은 것" 순이 아니라 **"나중에 하면 비싸지는 것"** 순으로 짭니다.

```
싸다 ←──────────────────────────────────────────→ 비싸다
디자인 마감      데이터 구조 변경      새 화면 추가      미션 설계
(화면 5개일 때)  (화면 5개일 때)      (구조 확정 후)    (기능 확정 후)
```

**핵심 규칙 두 가지**

1. **데이터 구조를 바꾸는 일은 화면이 적을 때 한다.**
   퇴사자 처리를 나중에 하면 급여 화면까지 전부 다시 고쳐야 합니다.
2. **채점(미션)은 맨 마지막에 설계한다.**
   기능이 바뀔 때마다 채점식이 깨지기 때문입니다.

---

## 2. 단계별 계획

### 1단계 — 디자인 마감 (design.md 잔여분) ✅ **완료 (2026-09-14)**

**왜 이걸 먼저 했나**
앞으로 만들 급여 화면·퇴사자 화면이 **같은 디자인 규칙 위에서** 만들어져야 합니다.
새 화면을 먼저 만들고 디자인을 바꾸면 두 번 일하게 됩니다.

**한 일**

| # | 작업 | 결과 |
| --- | --- | --- |
| 1 | 본문을 `design.md`의 17px 원칙으로 올림 | 본문·설명·입력폼 = **17px**, 표·버튼·필터 = **14px**(caption) |
| 2 | 서체 사다리 정리 | 사다리에 없던 11.5px·11px·20px 총 **18곳**을 12px·21px로 교정 |
| 3 | 교육용 안내 띠에 backdrop blur | `rgba(39,39,41,.82)` + `blur(20px) saturate(180%)`, 미지원 브라우저는 불투명 폴백 |
| 4 | 근태표 주말 음영 수정 | `#fafafc`는 흰색과 구분이 안 돼 **`#ececed`**로 교체 |
| 5 | 대시보드 표 줄바꿈 수정 | 부서·휴가종류·기간이 두 줄로 깨지던 것 `nowrap` 처리 |
| 6 | 초안 흔적 정리 | 임시 함수명 `challenge_placeholder_row` → `pendingRow` |

**Inter 웹폰트 — 안 쓰기로 결정**
`design.md`는 "`system-ui, -apple-system`을 스택 첫머리에 두고, Inter는 **비(非)Apple 플랫폼용 대체**"라고 합니다.
Windows 11의 `system-ui`는 Segoe UI Variable로, SF Pro와 같은 "시스템 서체" 역할을 합니다.
현재 스택에 `Inter`가 이미 들어 있어 **설치돼 있으면 자동으로 쓰이고, 없으면 시스템 서체로 대체**됩니다.
웹폰트를 새로 받아오면 **오프라인 단일 파일 원칙**이 깨지므로 추가하지 않습니다.

**완료 기준 통과 확인 (브라우저 실측)**

| 게이트 | 결과 |
| --- | --- |
| 초기화 → PRD 6-2 숫자 복원 | 결재대기 8건 · 반납지연 3건 · 재고부족 1종 ✅ |
| 기초 미션 8개 재달성 | **8 / 8** ✅ |
| 다시 초기화 | 0 / 8 + 기준 숫자 복원 ✅ |
| 교육용 표시 4곳 | 모두 정상 ✅ |
| 콘솔 오류 | 없음 (favicon 404만) ✅ |

---

### 2단계 — 퇴사자 처리 (재직상태)

**무엇을**
- 직원 자료에 이미 있는 `재직상태` 항목을 앱이 실제로 쓰게 만들기
- 퇴사자는 **드롭다운(신청자·사용자 선택)에서 빠지되**, 과거 기록에는 그대로 남기
- 근태·휴가 목록에 `퇴사` 표시
- 테스트용 퇴사자 1~2명을 가상 자료에 추가

**왜 지금**
이 변경은 **직원 목록을 쓰는 모든 곳**(휴가 신청 폼, 불출 폼, 근태표, 집계)에 닿습니다.
화면이 5개인 지금이 가장 쌉니다. 급여 화면이 생긴 뒤에 하면 고칠 곳이 늘어납니다.

**완료 기준**
- 퇴사자가 신규 신청 드롭다운에 안 나온다
- 퇴사자의 **과거 휴가·불출 기록은 그대로 보인다** (`데이터안내.md` 규칙 3)

---

### 3단계 — 급여 조회 화면 (PRD 미결정 #1)

**무엇을**
- 급여대장 목록 + 개인별 명세서 미리보기 (**조회 전용, 입력 불가**)
- 화면 위에 **공제율을 크게 표시**하고 `가상 율` 배지를 붙임
- 계산 과정을 화면에 그대로 노출 (`데이터안내.md` 8번의 6줄 계산식)
- 자료는 `data/가상_인사총무.json`의 12건을 쓴다

**왜 지금**
자료와 계산식이 이미 검증돼 있고(지급 51,398,540원 / 공제 10.4%),
2단계에서 직원 구조가 확정된 뒤에 얹는 것이 안전합니다.

**⚠️ 반드시 지킬 것**
급여는 요율이 매년 바뀝니다. **요율을 코드에 숨기지 말고 화면에 드러내고,
"교육용 가상 율"이라고 명시**해야 합니다. (PRD 2-2에서 이 위험 때문에 미뤄둔 기능)

**완료 기준**
- 명세서 한 장의 모든 숫자가 화면의 계산식으로 설명된다
- 어느 화면에서도 "실제 급여"로 오해할 표현이 없다

---

### 4단계 — 내보내기 확장 (근태·급여 CSV)

**무엇을**
- 근태 현황표를 CSV로 내려받기 (`레퍼런스.md`의 flex 원천 데이터 다운로드 참고)
- 급여대장 CSV
- 엑셀에서 한글이 깨지지 않게 **UTF-8 BOM** 붙이기

**왜 지금**
읽기 전용이라 기존 기능을 깨뜨릴 위험이 거의 없고,
급여까지 나온 뒤에 하면 **두 개를 한 번에** 만들 수 있습니다.

**완료 기준** — 내려받은 CSV를 엑셀에서 열었을 때 한글이 안 깨진다

---

### 5단계 — 실습 미션 2단계

**무엇을**
- 기존 8개는 **기초 과정**으로 두고, **심화 미션**을 추가
  - 예: 퇴사자의 미반납 비품 찾아 회수 처리하기
  - 예: 연장근로가 가장 많은 직원 찾기
  - 예: 같은 날 같은 팀에서 2명 이상 휴가 신청한 건 골라내기
- 기초/심화 전환 버튼

**왜 마지막**
채점식은 **기능이 확정된 뒤에** 짜야 합니다.
2~4단계에서 화면이 바뀌면 미션 채점이 전부 깨집니다.

**완료 기준** — 심화 미션도 자동 채점되고, 초기화하면 0으로 돌아간다

---

### 6단계 — 문서 동기화와 최종 점검

**무엇을**
- `PRD.md` 8번 미결정 항목 정리 (급여·디자인 결론 반영)
- `데이터안내.md` 7번 구현 현황표 갱신
- 6-2 기준 숫자 재측정 후 문서에 반영
- 화면 캡처를 `docs/screens/`에 갱신

**완료 기준** — 문서에 적힌 숫자와 앱이 보여주는 숫자가 같다

---

## 3. 단계마다 반복할 점검 (게이트)

한 단계를 끝낼 때마다 **매번** 아래를 통과해야 다음으로 갑니다.

| # | 점검 | 통과 기준 |
| --- | --- | --- |
| 1 | 초기화 | PRD 6-2 숫자(8건 / 3건 / 1종)로 정확히 복원 |
| 2 | 미션 | 기초 8개가 여전히 8/8 달성 가능 |
| 3 | 새로고침 | 작업 내용이 유지됨 |
| 4 | 교육용 표시 | 상단 띠 · 사이드바 배지 · 하단 문구 모두 보임 |
| 5 | 콘솔 | 오류 없음 (favicon 404는 무시) |

---

## 4. design.md 적용 현황

| design.md 규칙 | 상태 | 비고 |
| --- | --- | --- |
| 단일 액션 블루 `#0066cc` | ✅ 적용 | 모든 주요 버튼·링크·뱃지 |
| 근블랙 잉크 `#1d1d1f` | ✅ 적용 | 본문·제목 |
| 파치먼트 캔버스 `#f5f5f7` | ✅ 적용 | 페이지 바탕 |
| 다크 타일 `#272729` / `#2a2a2c` | ✅ 적용 | 사이드바·안내 띠·토스트 |
| **그림자 0개** | ✅ 적용 | `box-shadow` 사용 **0회** — 헤어라인과 면색으로만 위계 |
| 알약(pill) CTA | ✅ 적용 | 승인·반납·등록 버튼 |
| 카드 라운드 18px | ✅ 적용 | 모든 카드·모달 |
| 유틸 캡슐 11px | ✅ 적용 | 보조 버튼·입력 |
| 누름 시 `scale(.95)` | ✅ 적용 | Apple 시그니처 마이크로 인터랙션 |
| 디스플레이 음수 자간 | ✅ 적용 | 제목 `-.374px` |
| 모달 backdrop blur | ✅ 적용 | 반려 사유 입력창 |
| sticky bar backdrop blur | ✅ 적용 | 교육용 안내 띠 (1단계) |
| **본문 17px** | ✅ 적용 | 본문·설명·입력폼 17px / 표·버튼은 caption 14px (1단계) |
| 서체 사다리 (10·12·14·17·21·28·34·40) | ✅ 적용 | 사다리 밖 18곳 교정 (1단계) |
| **Inter 웹폰트** | ❌ 미적용 | **결론.** 웹폰트는 인터넷 필요 → 오프라인 원칙과 충돌. `system-ui`(Windows=Segoe UI Variable)로 충분하고, Inter가 설치돼 있으면 자동 사용 |
| **섹션 여백 80px** | ❌ 미적용 | 업무 시스템은 한 화면에 정보가 많아야 함 |
| **밝은/어두운 타일 교대** | ❌ 미적용 | 제품 사진이 없어 리듬이 성립하지 않음 |
| 검색 입력 알약 | — | 앱에 검색 입력이 없음 |
| **의미색(초록·주황·빨강)** | ➕ 추가 | design.md에 없는 색. **지연·결근·재고부족을 구분하려면 필수** → 채도를 낮춰 블루를 침범하지 않게 함 |

> **왜 일부를 안 따랐나**
> `design.md`는 **제품 사진을 보여주는 저밀도 소개 페이지**의 규칙입니다.
> 이 앱은 **표를 읽는 고밀도 업무 화면**이라, 여백·타일 교대·본문 크기는 그대로 쓰면 못 쓰는 화면이 됩니다.
> 그래서 **색·서체·모양·그림자 규칙은 그대로 따르고, 밀도 규칙만 업무용으로 바꿨습니다.**

---

## 5. 이번에도 하지 않는 것

| 제외 | 이유 |
| --- | --- |
| 로그인·권한 분리 | 교육용이라 전원 인사담당자 시점 |
| 실제 메일·알림 발송 | 외부로 나가는 동작은 만들지 않음 |
| 연차 이월·촉진 | 회사마다 규칙이 달라 교육용으로 부적합 |
| 승인 취소·결재 회수 | "한 번 누르면 끝"이 주는 긴장감이 학습에 도움 (PRD 5-5) |
| 실제 4대보험 요율 반영 | 매년 바뀜 — 가상 율만 사용 |

---

## 6. 추가였던 DART 공시 검색 — 2026-09-15 제거

한때 여섯 번째 화면으로 금융감독원 전자공시(DART) 공시검색을 붙였다가,
이 앱은 **인사·총무 실습에만 집중하기로 해서 통째로 걷어냈습니다.**

| 무엇 | 어떻게 됐나 |
| --- | --- |
| 왼쪽 메뉴 `공시 검색` | `index.html` 에서 제거 (화면 5개로 복귀) |
| 화면·조회 코드 약 330줄 | `index.html` 에서 삭제 |
| 회사 개황용 `.kv` 전용 CSS | 같이 삭제 |
| 파란 `실제 자료` 안내 띠 | 삭제 — 이제 모든 화면이 노란 `교육용` 띠 |
| `dart_server.py`, `.cache/corpCode.xml` | `_미사용/` 폴더로 이동 (지우지는 않음) |

되살리고 싶으면 `_미사용/README.md` 를 보세요.
`.env` 의 `DART_API_KEY` 는 지금은 아무 데서도 쓰지 않습니다.
