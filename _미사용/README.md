# _미사용 (사용하지 않는 파일 보관함)

인사·총무 실습앱(`index.html`)과 직접 관계가 없어서 2026-09-15 에 여기로 옮긴 것들입니다.
**앞으로 이 폴더 안의 파일은 사용하지 않습니다.** (지우지는 않고 보관만 합니다.)

| 파일 | 원래 위치 | 무엇이었나 |
| --- | --- | --- |
| `gemini_test.py` | 프로젝트 루트 | Gemini API 호출 맛보기 스크립트. 실습앱과 무관. |
| `dart_server.py` | 프로젝트 루트 | DART 공시검색용 파이썬 중계 서버. |
| `.cache/CORPCODE.xml` | 프로젝트 루트 | `dart_server.py` 가 받아 둔 회사코드 파일(30MB). |
| `.playwright-mcp/` | 프로젝트 루트 | 브라우저 검증 중 자동 생성된 로그·화면구조 부산물. |

## 알아둘 점

`index.html` 의 **「공시 검색」 메뉴는 그대로 남겨 두었습니다.**
중계 서버(`dart_server.py`)가 여기로 빠졌기 때문에 그 화면은 지금 동작하지 않습니다.
다시 쓰고 싶으면 `dart_server.py` 를 프로젝트 루트로 되돌린 뒤
`python dart_server.py` 를 실행하면 됩니다.
