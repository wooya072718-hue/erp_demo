# PRD — 인사·총무 실무 학습 앱

> ⚠️ **교육용 가상 자료 프로젝트**
> 이 제품에 나오는 회사·직원·자산·급여는 전부 학습을 위해 처음부터 지어낸 허구입니다.
> 실존 인물·기업과 무관하며, 실제 인사·급여·회계 업무에 사용할 수 없습니다.

| 항목 | 내용 |
| --- | --- |
| 문서 버전 | v1.0 |
| 작성일 | 2026-09-14 |
| 관련 문서 | `PLAN.md` (기획) · `레퍼런스.md` (참고 제품) · `데이터안내.md` (데이터 설계) |
| 구현 상태 | `index.html` 동작 확인 완료 |

---

## 1. 목적

### 1-1. 왜 만드는가

인사·총무 담당자가 **실제 회사 시스템을 건드리지 않고** 근태·휴가 결재와
비품 불출·반납 처리를 연습할 수 있게 한다.
실수해도 안전하고, 언제든 처음 상태로 되돌릴 수 있어야 한다.

### 1-2. 누가 쓰는가

**신입 인사·총무 담당자 (자습)**
강사 없이 혼자 화면을 보며 따라 하는 상황을 기준으로 만든다.
그래서 "무엇을 해야 하는지"를 화면이 직접 알려줘야 한다 → **실습 미션 체크리스트**.

### 1-3. 성공했다고 보는 기준

1. 교육생이 **설명서 없이** 앱을 열어 실습 미션 8개를 끝낼 수 있다.
2. 교육생이 **"휴가를 승인하면 근태표가 바뀐다"**는 연결을 직접 눈으로 확인한다.
3. 교육생이 **"비품 재고는 입력하는 게 아니라 계산되는 값"**임을 이해한다.
4. 어떤 화면을 캡처해도 **교육용 가상 자료라는 표시가 함께 찍힌다.**

---

## 2. 범위

### 2-1. 이번에 만드는 것

| # | 화면 | 핵심 기능 |
| --- | --- | --- |
| 1 | 대시보드 | 오늘 처리할 일만 모아 보기 |
| 2 | 근태 현황 | 월별 직원 × 일자 표, 이상 근태 필터 |
| 3 | 휴가 관리 | 신청 등록, 1단계 승인/반려, 잔여 연차 |
| 4 | 비품 대장 | 품목별 재고(자동 계산), 부족 경고 |
| 5 | 불출·반납 | 불출 등록, 반납 처리, 지연 표시 |
| — | 실습 미션 | 8개 미션 자동 채점 (사이드바 상시 표시) |

### 2-2. 만들지 않는 것

| 제외 | 이유 |
| --- | --- |
| 급여 계산 화면 | 4대보험 요율·간이세액표가 매년 바뀌어 **틀린 숫자를 가르칠 위험**. 데이터와 설명 문서(`데이터안내.md` 8번)로만 다룸 |
| 로그인·권한 분리 | 교육용이라 전원 인사담당자 시점 |
| 채용·인사평가 | 근태·휴가·비품에 집중 |
| 실제 메일·알림 발송 | 외부로 나가는 동작은 만들지 않음 |
| 연차 이월·촉진 | 실무 규칙이 회사마다 달라 교육용으로 부적합 |
| 2단계 결재(팀장→인사팀) | 1단계로 확정 (`레퍼런스.md` 마지막 항목) |

---

## 3. 기능 요구사항

### F-01 대시보드
- 요약 카드 4개: **오늘 근무 인원 / 결재 대기 휴가 / 반납 지연 / 재고 부족**
- 결재 대기 휴가 목록에서 **바로 승인·반려** 가능
- 오늘 자리에 없는 사람(휴가·출장) 목록
- 반납 지연 목록에서 **바로 반납 처리** 가능

### F-02 근태 현황
- 직원(행) × 일자(열) 표. 주말은 회색, 공휴일은 분홍, 아직 오지 않은 날은 빈칸
- 상태 8종: 정상 · 지각 · 조퇴 · 결근 · 연차 · 반차 · 출장 · 병가/경조사
- 월 선택(2026-08 / 2026-09), 부서 필터
- **[이상 근태만 보기]** — 지각·조퇴·결근이 1회라도 있는 직원만 남김 *(flex 참고)*
- 상단 집계 카드: 지각 / 조퇴 / 결근 / 연차 사용 / 출장 건수
- 행 끝 개인 집계: 출근일수 · 지각 · 결근 · 연차

### F-03 휴가 관리
- 탭: **대기 / 승인 / 반려 / 전체** (건수 표시), 부서 필터
- 신규 신청: 신청자 · 종류 · 기간 · 사유
- **신청자를 고르면 즉시 `부여 / 사용 / 잔여` 표시** *(Odoo Time Off 참고)*
- 승인 → 상태·처리일·결재자 기록, 근태표에 자동 반영
- 반려 → **반려 사유 입력 필수**, 목록에 사유 표시

### F-04 비품 대장
- 목록: 자산코드 · 품목 · 규격 · 분류 · 총수량 · 불출 · **가용(자동 계산)** · 상태 · 보관위치 · 단가
- 상태: 재고없음(가용 0) / 부족주의(가용 1~2) / 정상
- 분류 필터, **[재고 부족만 보기]** 필터

### F-05 불출·반납
- 탭: 전체 / 지연 / 대여중 / 반납완료 / 소모품
- 신규 불출: 비품 · 사용자 · 수량 · 불출일 · 반납예정일(비우면 상시 지급) · 비고
- 반납 처리 → **기록을 지우지 않고 반납일만 채움** *(ERPNext Asset Movement 참고)*

### F-06 실습 미션 (자동 채점)

| # | 미션 | 채점 방법 |
| --- | --- | --- |
| 1 | 결재 대기 휴가 3건 승인 | 처음 '대기'였던 건이 '승인'이 된 개수 ≥ 3 |
| 2 | 휴가 1건 반려 (사유 필수) | '반려' + 반려사유가 비어 있지 않음 |
| 3 | 휴가 신청 1건 신규 등록 | 초기 자료에 없던 신청번호 존재 |
| 4 | 잔여 연차 초과 신청해 경고 확인 | 초과 경고가 뜬 적이 있음 |
| 5 | 이상 근태 직원만 걸러 보기 | 해당 필터를 켠 적이 있음 |
| 6 | 재고 바닥난 비품 찾기 | 해당 필터를 켠 적이 있음 |
| 7 | 반납 지연 2건 반납 처리 | 처음 지연이던 건에 반납일이 채워진 개수 ≥ 2 |
| 8 | 비품 불출 1건 신규 등록 | 초기 자료에 없던 불출번호 존재 |

### F-07 자료 저장·초기화
- 입력·수정 내용은 **브라우저 저장소(localStorage)** 에 저장 → 새로고침해도 유지
- **JSON 내려받기 / 불러오기** — 실습 결과를 파일로 주고받기
- **처음 상태로 초기화** — 확인 후 전부 되돌림

---

## 4. 데이터 요구사항

### 4-1. 자료가 두 벌인 이유 (역할 분담)

| 자료 | 위치 | 규모 | 역할 |
| --- | --- | --- | --- |
| **앱 실습용** | `index.html` 안 | 직원 24 · 휴가 18 · 비품 14 · 불출 17 | **앱을 돌려 보는 용도.** 미션을 풀 만큼 사례가 다양해야 해서 크게 만듦 |
| **구조 학습용** | `data/가상_인사총무.json` | 직원 12 · 휴가 6 · **급여 12** | **표와 연결을 손으로 따라가는 용도.** 작아야 눈으로 쫓을 수 있어서 줄임 |

두 자료는 **사원번호 체계(E001~)를 공유**한다. 12명은 24명 중에서 뽑았다.
→ `데이터안내.md`의 박도현(E003) 예시가 양쪽에서 똑같이 통한다.

### 4-2. 표와 키

| 표 | 키 | 한 줄의 의미 |
| --- | --- | --- |
| 직원 | `사원번호` | 사람 한 명 |
| 휴가 | `신청번호` | 휴가 신청 한 건 |
| 급여 | `급여번호` (= 사원번호 + 귀속월) | 한 사람의 한 달치 급여 |
| 비품 | `자산코드` | 품목 한 종류 |
| 불출 | `불출번호` | 불출 한 건 |

### 4-3. 연결 규칙

1. 사람을 가리킬 때는 **반드시 사원번호**를 쓴다. 이름은 동명이인·개명·오타 때문에 쓰지 않는다.
2. 직원 표에 없는 사원번호는 다른 표에 쓸 수 없다. → 화면에서는 **드롭다운으로만 선택**.
3. 퇴사자는 줄을 지우지 않고 `재직상태`를 바꾼다. 지우면 과거 기록이 주인을 잃는다.
4. **계산할 수 있는 값은 저장하지 않는다.**

| 저장하지 않음 | 계산식 |
| --- | --- |
| 잔여 연차 | 연차부여일수 − 승인된 연차·반차 일수 합계 |
| 비품 가용수량(내구재) | 총수량 − 반납 안 된 불출수량 |
| 비품 가용수량(소모품) | 총수량 − 불출한 수량 전부 |
| 지급총액 / 실지급액 | 기본급+수당 − 공제 |

---

## 5. 동작 규칙

문서를 검토하며 **명확히 적혀 있지 않던 규칙들**을 여기서 확정한다.

### 5-1. 날짜·기준일
- **`오늘`은 2026-09-14로 고정한다.** 실제 날짜를 쓰면 실습할 때마다 결과가 달라져 미션 채점이 흔들린다.
- 공휴일은 자료에 적힌 3일만 인정: 추석 연휴(9/24) · 추석(9/25) · 대체공휴일(9/28)
- 근무일 = 주말도 공휴일도 아닌 날

### 5-2. 휴가
- 결재는 **1단계**. `대기` → `승인` 또는 `반려`
- **한 번 처리한 건은 되돌릴 수 없다.** (승인 취소·회수 기능 없음 — 5-5 참고)
- **반려에는 사유가 반드시 있어야 한다.** 비우면 처리되지 않는다.
- 휴가 일수 = 기간 중 **근무일만** 센다 (주말·공휴일 제외)
- **반차는 0.5일**이고 종료일은 시작일과 같게 고정된다
- **연차 차감 대상은 `연차`와 `반차`뿐.** 병가·경조사는 별도로 집계하고 연차를 깎지 않는다
- **잔여 연차를 넘는 신청은 등록도 승인도 되지 않는다.** 경고를 띄우고 막는다
  - 신청 당시에는 잔여가 충분했어도, **승인 시점에 부족하면 승인이 거부된다** (다른 건이 먼저 승인된 경우)

### 5-3. 근태
- 승인된 휴가는 근태표에 **자동으로** 표시된다 (연차 `연` / 반차 `반` / 병가·경조사 `병`)
- 휴가가 아닌 날의 근태는 **사원번호 + 날짜로 계산해 생성**한다. 같은 사람·같은 날은 항상 같은 결과가 나온다
- **오늘 이후 날짜는 빈칸.** 아직 오지 않은 날의 근태는 만들지 않는다
- 영업팀은 출장 비율을 높게 잡는다 (부서마다 근태 모양이 달라야 배울 거리가 생긴다)

### 5-4. 비품·불출
- **가용수량은 입력하지 않는다.** 불출 이력에서 계산한다
- **소모품은 반납이 없다.** 불출하면 재고에서 영구히 빠진다
- 불출 수량은 **가용수량을 넘을 수 없다**
- 반납예정일이 비어 있으면 **상시 지급**으로 보고 **지연 판정 대상이 아니다** (업무용 노트북 등)
- 반납 지연 = 반납예정일이 있고 · 그날이 지났고 · 아직 반납되지 않음
- 반납 처리는 **기록을 지우지 않고 반납일만 채운다**

### 5-5. 지금은 없는 기능 (알고 비워 둔 곳)

| 없는 것 | 왜 |
| --- | --- |
| 승인 취소 / 결재 회수 | 실무엔 있지만, 교육용은 "한 번 누르면 끝"이라는 긴장감이 학습에 도움 |
| 퇴사자 처리 화면 | `재직상태` 항목은 설계에 있으나 앱 화면에는 아직 없음 |
| 비품 신규 등록 / 폐기 | 비품 목록 자체는 고정. 불출·반납만 연습 |
| 연차 이월·촉진 | 회사마다 규칙이 달라 제외 |
| 급여 화면 | 2-2 참고 |

---

## 6. 확인 기준

`확인예시.md`가 프로젝트에 없어 이 절로 대신한다.

### 6-1. 자료 무결성 (검증 완료 ✅)

| 확인 항목 | 기준 | 결과 |
| --- | --- | --- |
| 휴가·불출의 사원번호가 직원 표에 있는가 | 전부 있어야 함 | ✅ 통과 |
| 불출의 자산코드가 비품 표에 있는가 | 전부 있어야 함 | ✅ 통과 |
| 신청번호·불출번호 중복 | 없어야 함 | ✅ 없음 |
| 급여의 `사원번호 + 귀속월` 중복 | 없어야 함 (있으면 이중 지급) | ✅ 없음 |
| 급여 계산 일치 | 지급총액 − 공제총액 = 실지급액 | ✅ 12건 전부 일치 |

### 6-2. 초기 상태 숫자 (이 값이 나와야 정상)

| 항목 | 값 |
| --- | --- |
| 결재 대기 휴가 | **8건** |
| 반납 지연 | **3건** (N004 · N007 · N016) |
| 재고 없음 | **1종** (A-2410 빔프로젝터, 가용 0/2) |
| 부족 주의 | **1종** (A-2413 토너, 가용 1/3) |
| 급여 지급총액 합계 | **51,398,540원** |
| 급여 공제 합계 | **5,340,840원** (지급총액 대비 10.4%) |
| 급여 실지급 합계 | **46,057,700원** |

### 6-3. 동작 확인 (손으로 눌러 보는 항목)

| # | 이렇게 하면 | 이렇게 되어야 한다 |
| --- | --- | --- |
| 1 | 휴가 1건 승인 | 근태 현황의 해당 날짜가 `연`/`반`으로 바뀐다 |
| 2 | 반려 사유를 비우고 [반려 처리] | 처리되지 않고 빨간 경고가 뜬다 |
| 3 | 잔여 연차보다 긴 기간으로 신청 | 등록되지 않고 `신청 N일 > 남은 연차 M일` 경고가 뜬다 |
| 4 | 반납 지연 건 [반납 처리] | 목록에서 사라지지 않고 `반납 09-14`로 바뀐다 |
| 5 | 비품을 가용수량보다 많이 불출 | 등록되지 않고 경고가 뜬다 |
| 6 | 소모품을 불출 | 반납 대상이 아니고 재고가 바로 줄어든다 |
| 7 | 새로고침 | 방금 한 작업이 그대로 남아 있다 |
| 8 | [처음 상태로 초기화] | 6-2의 숫자로 정확히 돌아온다 |
| 9 | 미션 8개를 다 끝냄 | 사이드바에 `8 / 8`과 축하 문구가 뜬다 |

### 6-4. 교육용 표시 (반드시 지켜야 함)

| 위치 | 확인 |
| --- | --- |
| 화면 맨 위 | 노란 줄무늬 띠에 "교육용 / 가상 자료" 문구 — 스크롤해도 계속 보임 |
| 사이드바 | `TRAINING / 가상` 배지 |
| 모든 화면 하단 | "(주)한빛상사는 실존하지 않는 가상 회사" 문구 |
| 모든 문서 첫머리 | ⚠️ 교육용 가상 자료 안내 |
| 내려받은 JSON | `_주의` 항목에 경고 문구 포함 |

---

## 7. 디자인 기준

- 현재: 업무 시스템 느낌의 **파란색 계열 + 좌측 사이드바** 구성
- 참고 자료로 `references/Cal.reference.md` (Cal.com 디자인 분석, 542줄)를 받아 뒀다
  - 흰 바탕 · 검정 CTA · 넉넉한 여백 · 12px 둥근 카드
- **이번 버전에서는 전면 재도색을 하지 않는다.** 업무 시스템은 정보 밀도가 높아야 하고,
  Cal.com은 여백이 넓은 소개 페이지 스타일이라 근태표 같은 화면과 맞지 않는다
- 다만 **여백·둥근 모서리·버튼 위계**는 참고해서 다듬는다

---

## 8. 미결정 사항

아래 두 가지는 확답을 받지 못해 **기본값으로 진행**했다. 바꾸고 싶으면 말씀만 주면 된다.

| # | 항목 | 정한 기본값 | 바꾸면 생기는 일 |
| --- | --- | --- | --- |
| 1 | 급여 화면 | **만들지 않음** (2-2) | 만들면 조회 전용 화면 1개 추가 + 요율 수정 기능 여부 재결정 |
| 2 | Cal.com 디자인 적용 범위 | **참고만, 재도색 없음** (7) | 전면 적용하면 5개 화면 스타일을 다시 잡아야 함 |

---

## 9. 다음 단계 후보

1. 급여 조회 화면 추가 (요율을 화면에서 고칠 수 있게)
2. 퇴사자 처리 — `재직상태` 화면 반영
3. 근태 엑셀 내려받기 (flex의 원천 데이터 다운로드 참고)
4. 실습 미션 2단계 — 난이도 높은 시나리오 추가
